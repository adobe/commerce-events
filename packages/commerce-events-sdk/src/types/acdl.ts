/**
 * The ACDL obviously have a lot more types than this but this will eventually be phased out to take a more generic
 * approach so we can dynamically load any data layer manager that our sdk should be able to use.
 */
export type AdobeClientDataLayer = any;
